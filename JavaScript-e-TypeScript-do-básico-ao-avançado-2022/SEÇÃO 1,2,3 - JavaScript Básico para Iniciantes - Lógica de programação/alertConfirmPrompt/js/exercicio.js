// fazer uma soma com os números pegos do prompt

const num1 = Number(prompt('Digite o primeiro número da soma.'));
const num2 = Number(prompt('Digite o segundo valor da soma.'));
let soma = num1 + num2
console.log(soma)